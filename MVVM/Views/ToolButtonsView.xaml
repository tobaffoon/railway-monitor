<UserControl x:Class="railway_monitor.MVVM.Views.ToolButtonsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:railway_monitor.MVVM.Views"
             xmlns:viewmodels="clr-namespace:railway_monitor.MVVM.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="auto" d:DesignWidth="auto">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.Resources>
            <viewmodels:ToolButtonsViewModel x:Key="ToolButtonsViewModel"/>
        </Grid.Resources>

        <ItemsControl Grid.Column="1" Grid.Row="0" ItemsSource="{Binding Source={StaticResource ToolButtonsViewModel}, Path=ToolButtons}"></ItemsControl>

    </Grid>
</UserControl>
